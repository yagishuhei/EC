<h1>ジャンル一覧・追加</h1>
<%= form_with model: [:admin, @genre] do |f| %>

<%= f.text_field :name %>
<%= f.submit '新規投稿' %>
<% end %>

<% @genres.each do |genre| %>
<%= genre.name %>
<%= link_to '編集する', edit_admin_genre_path(genre.id) %>
<%= link_to '削除する', 
<% end %>